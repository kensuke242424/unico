# テストコミット

このファイルは、Git Hook のテスト用に作成されました。

## 確認事項

- [x] ブランチ名: `bugfix/#64-test-branch`
- [ ] コミットメッセージに自動で `[#64]` が追加されるか確認

---

**期待される動作:**
コミット時に自動で `[#64]` がメッセージの先頭に追加される。

例:
```
git commit -m "Git Hookのテスト"
→ [#64] Git Hookのテスト
```
